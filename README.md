API MASTER (Para crear cualquier API usar este maestro)

- Cuenta con accounts y users
- Comandos:
	- composer install
	- php artisan key:generate (luego de modificar el .env)
	- php artisan migrate
	- generar storage link: php artisan storage:link